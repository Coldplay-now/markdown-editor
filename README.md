# 📝 Markdown 编辑器

一个功能强大的 Web Markdown 编辑器，支持实时预览、代码高亮、数学公式、Mermaid 图表等功能。

## ✨ 功能特性

### 核心功能
- ✅ **实时预览**：左右分栏布局，编辑和预览同步显示
- ✅ **Markdown 渲染**：支持 GitHub Flavored Markdown (GFM)
- ✅ **代码高亮**：支持多种编程语言的语法高亮
- ✅ **数学公式**：使用 KaTeX 渲染行内和块级数学公式
- ✅ **Mermaid 图表**：支持流程图、序列图、饼图等多种图表
- ✅ **目录生成**：使用 `[TOC]` 自动生成文档目录并支持跳转

### 编辑体验
- ✅ **智能插入**：在光标位置插入格式，选中文本智能包裹
- ✅ **滚动同步**：编辑器与预览区域双向滚动同步
- ✅ **美观图标**：使用 Lucide React 图标库，统一现代
- ✅ **快捷工具栏**：一键插入常用 Markdown 语法
- ✅ **复制粘贴**：支持一键复制全部内容和粘贴
- ✅ **Tab 缩进**：编辑器内支持 Tab 键插入缩进

### 图片与文件
- ✅ **图片支持**：可插入在线图片或上传本地图片（转 Base64）
- ✅ **文件下载**：支持导出为 .md 文件
- ✅ **自动保存**：内容自动保存到浏览器 localStorage（1秒防抖）

### 其他特性
- ✅ **响应式设计**：支持桌面和移动设备
- ✅ **现代化 UI**：渐变色标题、悬停效果、流畅动画

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 🛠️ 技术栈

- **React 18** - UI 框架
- **Vite** - 构建工具和开发服务器
- **react-markdown** - Markdown 渲染引擎
- **remark-gfm** - GitHub Flavored Markdown 支持
- **remark-math + rehype-katex** - 数学公式渲染
- **mermaid** - 图表渲染（流程图、序列图等）
- **react-syntax-highlighter** - 代码语法高亮
- **lucide-react** - 现代化图标库
- **KaTeX** - 数学公式排版

## 📖 使用说明

### 基础操作

1. **编辑内容**：在左侧编辑器中输入 Markdown 文本
2. **查看预览**：右侧实时显示渲染后的效果
3. **自动保存**：内容会自动保存到浏览器本地存储（1秒防抖）
4. **快速插入**：使用工具栏按钮快速插入常用语法

### 工具栏功能

#### 格式按钮（智能插入）
- **H1/H2/H3**：插入标题，在光标位置添加
- **粗体（B）**：选中文本变粗体，或插入 `**粗体文字**`
- **斜体（I）**：选中文本变斜体，或插入 `*斜体文字*`
- **删除线**：选中文本添加删除线，或插入 `~~删除文字~~`
- **链接**：选中文本变链接，或插入 `[链接文字](url)`
- **代码块**：插入代码块模板
- **引用**：插入引用前缀 `>`
- **列表**：无序列表（`-`）、有序列表（`1.`）、任务列表（`- [ ]`）
- **表格**：插入表格模板
- **分割线**：插入水平分割线 `---`
- **数学公式**：插入 LaTeX 公式块
- **Mermaid**：插入图表模板
- **目录（TOC）**：插入 `[TOC]` 标记，自动生成文档目录

#### 图片与文件
- **图片 URL**：插入在线图片链接
- **上传图片**：选择本地图片，自动转 Base64 嵌入

#### 编辑操作
- **复制**：一键复制全部内容到剪贴板
- **粘贴**：从剪贴板粘贴到光标位置

#### 文件操作
- **清空**：清空所有内容（需确认）
- **下载**：导出为 .md 文件

### 支持的 Markdown 语法

#### 标题
```markdown
# 一级标题
## 二级标题
### 三级标题
```

#### 文本样式
```markdown
**粗体**
*斜体*
~~删除线~~
```

#### 列表
```markdown
- 无序列表项
- 另一项

1. 有序列表项
2. 另一项

- [ ] 任务列表
- [x] 已完成任务
```

#### 链接和图片
```markdown
[链接文字](https://example.com)
![图片描述](图片URL)
```

#### 代码
```markdown
行内代码：`console.log()`

代码块：
\`\`\`javascript
function hello() {
  console.log("Hello, World!");
}
\`\`\`
```

#### 数学公式
```markdown
行内公式：$E = mc^2$

块级公式：
$$
\frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$
```

#### Mermaid 图表
```markdown
\`\`\`mermaid
graph TD
    A[开始] --> B[结束]
\`\`\`
```

#### 表格
```markdown
| 列1 | 列2 |
|-----|-----|
| 内容1 | 内容2 |
```

#### 目录生成
```markdown
[TOC]

# 第一章
## 1.1 节
## 1.2 节

# 第二章
## 2.1 节
```
会自动生成可点击的文档目录

## 📁 项目结构

```
markdown-editor/
├── src/
│   ├── components/
│   │   ├── Editor.jsx      # 编辑器组件
│   │   ├── Preview.jsx     # 预览组件
│   │   └── Toolbar.jsx     # 工具栏组件
│   ├── utils/
│   │   └── storage.js      # localStorage 工具函数
│   ├── App.jsx             # 主应用组件
│   ├── App.css             # 样式文件
│   ├── main.jsx            # 入口文件
│   └── index.css           # 全局样式
├── index.html              # HTML 模板
├── package.json            # 项目配置
└── vite.config.js          # Vite 配置
```

## 🎨 自定义

### 修改主题颜色

编辑 `src/App.css` 中的以下部分：

```css
.app-header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### 修改代码高亮主题

编辑 `src/components/Preview.jsx`，替换导入的主题：

```javascript
import { tomorrow } from 'react-syntax-highlighter/dist/esm/styles/prism';
// 可以替换为其他主题，如：
// import { atomDark } from 'react-syntax-highlighter/dist/esm/styles/prism';
```

### 修改 Mermaid 主题

编辑 `src/components/Preview.jsx` 中的 Mermaid 初始化：

```javascript
mermaid.initialize({
  startOnLoad: true,
  theme: 'default', // 可选: 'default', 'forest', 'dark', 'neutral'
  securityLevel: 'loose',
});
```

## 🔧 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 💡 使用技巧

### 编辑技巧
- 💾 内容会自动保存到浏览器 localStorage，不用担心丢失
- ⌨️ 支持 Tab 键在编辑器中插入缩进（2空格）
- 📝 选中文本后点击格式按钮，可以智能包裹格式
- 🖱️ 点击格式按钮会在光标位置插入，没有光标则在末尾添加
- 🔄 左右编辑器和预览区域支持双向滚动同步

### 图片处理
- 🖼️ 上传的图片会转换为 Base64 编码嵌入 Markdown
- 📎 Base64 图片适合小图，大图建议使用图床链接

### 目录功能
- 📑 在文档任意位置输入 `[TOC]` 可生成目录
- 🔗 点击目录项可跳转到对应标题
- 📊 目录会自动识别文档中的所有标题（H1-H6）

### 其他
- 🗑️ 可以使用浏览器的开发者工具清除 localStorage 来重置内容
- ⚡ 所有操作都在本地完成，无需网络连接

---

**开始使用吧！** 🚀 在 http://localhost:5173 查看应用
