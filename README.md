# 📝 Markdown 编辑器

一个功能强大的 Web Markdown 编辑器，支持实时预览、代码高亮、数学公式、Mermaid 图表等功能。

## ✨ 功能特性

- ✅ **实时预览**：左右分栏布局，编辑和预览同步显示
- ✅ **Markdown 渲染**：支持 GitHub Flavored Markdown (GFM)
- ✅ **代码高亮**：支持多种编程语言的语法高亮
- ✅ **数学公式**：使用 KaTeX 渲染行内和块级数学公式
- ✅ **Mermaid 图表**：支持流程图、序列图、饼图等多种图表
- ✅ **图片支持**：可插入在线图片或上传本地图片（转 Base64）
- ✅ **本地存储**：内容自动保存到浏览器 localStorage
- ✅ **工具栏**：快速插入常用 Markdown 语法
- ✅ **文件下载**：支持导出为 .md 文件
- ✅ **响应式设计**：支持桌面和移动设备

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 🛠️ 技术栈

- **React 18** - UI 框架
- **Vite** - 构建工具
- **react-markdown** - Markdown 渲染
- **remark-gfm** - GitHub Flavored Markdown 支持
- **remark-math + rehype-katex** - 数学公式渲染
- **mermaid** - 图表渲染
- **react-syntax-highlighter** - 代码语法高亮

## 📖 使用说明

### 基础操作

1. **编辑内容**：在左侧编辑器中输入 Markdown 文本
2. **查看预览**：右侧实时显示渲染后的效果
3. **自动保存**：内容会自动保存到浏览器本地存储（1秒防抖）
4. **快速插入**：使用工具栏按钮快速插入常用语法

### 工具栏功能

- **标题按钮**：插入 H1、H2、H3 标题
- **文本样式**：粗体、斜体、删除线
- **链接和图片**：插入链接和图片
- **代码块**：插入代码块
- **列表**：无序列表、有序列表、任务列表
- **表格**：插入表格模板
- **数学公式**：插入数学公式块
- **Mermaid**：插入 Mermaid 图表模板
- **图片上传**：上传本地图片（转为 Base64）
- **清空**：清空所有内容
- **下载**：下载为 .md 文件

### 支持的 Markdown 语法

#### 标题
```markdown
# 一级标题
## 二级标题
### 三级标题
```

#### 文本样式
```markdown
**粗体**
*斜体*
~~删除线~~
```

#### 列表
```markdown
- 无序列表项
- 另一项

1. 有序列表项
2. 另一项

- [ ] 任务列表
- [x] 已完成任务
```

#### 链接和图片
```markdown
[链接文字](https://example.com)
![图片描述](图片URL)
```

#### 代码
```markdown
行内代码：`console.log()`

代码块：
\`\`\`javascript
function hello() {
  console.log("Hello, World!");
}
\`\`\`
```

#### 数学公式
```markdown
行内公式：$E = mc^2$

块级公式：
$$
\frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$
```

#### Mermaid 图表
```markdown
\`\`\`mermaid
graph TD
    A[开始] --> B[结束]
\`\`\`
```

#### 表格
```markdown
| 列1 | 列2 |
|-----|-----|
| 内容1 | 内容2 |
```

## 📁 项目结构

```
markdown-editor/
├── src/
│   ├── components/
│   │   ├── Editor.jsx      # 编辑器组件
│   │   ├── Preview.jsx     # 预览组件
│   │   └── Toolbar.jsx     # 工具栏组件
│   ├── utils/
│   │   └── storage.js      # localStorage 工具函数
│   ├── App.jsx             # 主应用组件
│   ├── App.css             # 样式文件
│   ├── main.jsx            # 入口文件
│   └── index.css           # 全局样式
├── index.html              # HTML 模板
├── package.json            # 项目配置
└── vite.config.js          # Vite 配置
```

## 🎨 自定义

### 修改主题颜色

编辑 `src/App.css` 中的以下部分：

```css
.app-header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### 修改代码高亮主题

编辑 `src/components/Preview.jsx`，替换导入的主题：

```javascript
import { tomorrow } from 'react-syntax-highlighter/dist/esm/styles/prism';
// 可以替换为其他主题，如：
// import { atomDark } from 'react-syntax-highlighter/dist/esm/styles/prism';
```

### 修改 Mermaid 主题

编辑 `src/components/Preview.jsx` 中的 Mermaid 初始化：

```javascript
mermaid.initialize({
  startOnLoad: true,
  theme: 'default', // 可选: 'default', 'forest', 'dark', 'neutral'
  securityLevel: 'loose',
});
```

## 🔧 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 💡 提示

- 内容会自动保存到浏览器 localStorage，不用担心丢失
- 支持 Tab 键在编辑器中插入缩进
- 上传的图片会转换为 Base64 编码嵌入 Markdown
- 可以使用浏览器的开发者工具清除 localStorage 来重置内容

---

**开始使用吧！** 🚀 在 http://localhost:5173 查看应用
